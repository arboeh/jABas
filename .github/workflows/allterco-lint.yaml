name: ALLTERCO Lint
on:
  push:
    branches: [main]
  pull_request:
    paths: ['scripts/**', 'examples-manifest.json']

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python tools/check-manifest-integrity.py --check-all
